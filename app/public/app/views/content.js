define(['backbone', 'underscore'],
        function (Backbone) {
    'use strict';

    /**
     * @class ContentView
     * @extends Backbone.View
     * @desc Feed
     */
    var ContentView = Backbone.View.extend({
        initialize: function () {
            this.render();
        },

        render: function () {

        }
    });

    return ContentView;
});
